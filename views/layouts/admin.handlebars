{{> admin/admin-header}}
<body class="sb-nav-fixed">
{{> admin/admin-topnav}}
<div id="layoutSidenav">
    {{> admin/admin-sidenav}}
    <div id="layoutSidenav_content">
        {{> admin/messages}}
      {{{ body }}}
        {{> admin/admin-footer}}


